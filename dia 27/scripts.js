function llargaCurta() {
    let paraules = ["gat", "gos", "peix", "ocell", "serp"];

    let masLarga = paraules[0];
    let masCorta = paraules[0];

    for (let i = 1; i < paraules.length; i++) {
        // comparamos longitudes
    }

    // for (let palabra of paraules) {
    //     // comparamos longitudes
    // }

    document.getElementById('paraulaLlarga').innerHTML = masLarga;
    document.getElementById('paraulaCurta').innerHTML = masCorta;
}

